docker.enabled = true

params {
    reads = "${projectDir}/data/*_{R1,R2}.fastq.gz"
}

process {

    withName: FASTQC {
        cpus = 12
    }

}

manifest {
    name = 'nf-human-variants'
    description = 'Nextflow DSL2 pipeline for human germline variant calling'
    version = '0.1.0'
}